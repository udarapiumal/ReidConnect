`# ==========================================
# DEV PROFILE - Local development settings
# ==========================================

# Database (local PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/reidconnect
spring.datasource.username=postgres
spring.datasource.password=AsdBnm12#

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT secret (dev only — do NOT use in production)
security.jwt.secret-key=t9Z7nX2RgY4PvBQjM1vWhkUsaLF8e0Cd

# Mail credentials (dev only)
spring.mail.username=udarapiumal0@gmail.com
spring.mail.password=yhzq bkhv oavo odor

# Static resource serving (dev uses local src path)
spring.web.resources.static-locations=file:./uploads/

# CORS allowed origins (dev)
app.cors.allowed-origins=http://localhost:4200,http://localhost:3000,http://localhost:3001,http://localhost:8080,http://localhost:8081,http://localhost:8082
